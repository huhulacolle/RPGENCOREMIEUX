<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rpgencoremieux">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RPGENCOREMIEUX">
        <activity
            android:name=".Chapitre_1"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_chapitre_1"
            android:theme="@style/Theme.RPGENCOREMIEUX.Fullscreen"></activity>
        <activity
            android:name=".Debut"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_debut"
            android:theme="@style/Theme.RPGENCOREMIEUX.Fullscreen" />
        <activity android:name=".MenuPrincipale">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>